<p>[ 解析 ]</p>
<p class="detail">
    <%= questionDto.explanation %>
</p>
<div class="explanImg-content">
  <% if (questionDto.explanImgs && questionDto.explanImgs.length > 0) { %>
    <% questionDto.explanImgs.forEach(img => { %>
      <img src="/assets/img/<%= img.fileName %>" class="explan-img" />
    <% }); %>
  <% } %>   
</div>